class Trip {
    constructor(destination, imageUrl, departureDate, returnDate) {
        this.destination = destination
        this.imageUrl = imageUrl
        this.departureDate = departureDate
        this.returnDate = returnDate
    }
}

module.exports = Trip 